import { styles } from "./style";

type Props = {
  id: string;
  title: string;
  nameIcon?: string;
};

export function Card({ id, title, nameIcon }: Props) {
  const id = Math.random();
  return {};
}
